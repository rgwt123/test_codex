# GitHub 驱动的个人博客

这是一个纯前端的个人博客页面，直接从公开的 GitHub 仓库中加载 Markdown 文章，支持在线预览、简单编辑与导出。你可以在 GitHub 上传/更新文章，也可以在页面生成草稿并下载后提交到仓库。

## 主要特性
- **零后端依赖**：仅使用浏览器访问 GitHub API 和 raw 内容，适合 GitHub Pages 等静态托管方案。
- **文章目录可配置**：填写仓库信息后自动列出指定路径下的 `.md` 文件。
- **在线预览与编辑**：在页面中直接编辑 Markdown，实时 HTML 预览。
- **导出工具**：一键下载当前 Markdown 或复制渲染后的 HTML，便于上传到 GitHub 或发布到其他平台。

## 使用方法
1. 打开 `index.html`，在页面左侧填写 GitHub 用户/组织、仓库名和文章目录（默认 `posts`）。
2. 点击“加载文章列表”后会列出该目录下的所有 Markdown 文件，选择即可读取内容并进入编辑器。
3. 需要新文章时点击“新建空白草稿”，编辑完成后使用“下载 Markdown”将文件保存并上传到仓库。
4. 如果想发布 HTML，可使用“复制 HTML”按钮获取渲染后的内容。

- 如果遇到匿名访问频率限制，可在左侧“GitHub Token”中填入临时的个人访问令牌（仅保存在当前浏览器会话，不会发送到其他地方），即可提升读取和列表接口的配额。

> 提示：未登录 GitHub API 时会受到匿名速率限制，如遇到 403/429 可稍后再试或在浏览器中登录 GitHub 后刷新页面。
